diff --git a/esp32/lib/ble_navigation/workout_zone_protocol.hpp b/esp32/lib/ble_navigation/workout_zone_protocol.hpp
index 0f06c0a..3c69316 100644
--- a/esp32/lib/ble_navigation/workout_zone_protocol.hpp
+++ b/esp32/lib/ble_navigation/workout_zone_protocol.hpp
@@ -67,7 +67,7 @@ inline bool decode(const uint8_t *data, std::size_t size, Packet &out) {
   static_assert(sizeof(double) == 8 && std::numeric_limits<double>::is_iec559,
                 "zone thresholds require IEEE-754 binary64");
   if (!data || size < HEADER_BYTES || size > MAXIMUM_FRAME_BYTES ||
-      data[0] != FRAME_KIND || data[1] != VERSION ||
+      data[0] != FRAME_KIND || data[1] != ZONE_WIRE_VERSION ||
       (data[2] != METRIC_HEART_RATE && data[2] != METRIC_CYCLING_POWER) ||
       data[3] > SOURCE_HEALTHKIT_UNKNOWN || (data[4] & ~uint8_t(7)) != 0 ||
       (data[7] & 0x3F) == 0 || (data[7] & 0x3F) > 6) return false;
diff --git a/esp32/lib/ble_navigation/workout_zone_wire.generated.hpp b/esp32/lib/ble_navigation/workout_zone_wire.generated.hpp
index 041fce8..760b8bf 100644
--- a/esp32/lib/ble_navigation/workout_zone_wire.generated.hpp
+++ b/esp32/lib/ble_navigation/workout_zone_wire.generated.hpp
@@ -3,7 +3,7 @@
 #include <cstddef>
 #include <cstdint>
 namespace workout_zone_wire {
-inline constexpr uint32_t VERSION = 1;
+inline constexpr uint32_t ZONE_WIRE_VERSION = 1;
 inline constexpr uint32_t FRAME_KIND = 5;
 inline constexpr uint32_t HEADER_BYTES = 32;
 inline constexpr uint32_t MAXIMUM_ZONES = 9;
diff --git a/esp32/tools/tests/test_workout_zone_protocol.cpp b/esp32/tools/tests/test_workout_zone_protocol.cpp
index a07bf35..4c1e42b 100644
--- a/esp32/tools/tests/test_workout_zone_protocol.cpp
+++ b/esp32/tools/tests/test_workout_zone_protocol.cpp
@@ -1,3 +1,9 @@
+// Match the real firmware's build-identity macro so generated wire identifiers
+// cannot accidentally collide with VERSION again.
+#ifndef VERSION
+#define VERSION "host-firmware-identity"
+#endif
+
 #include "../../lib/ble_navigation/workout_zone_protocol.hpp"
 #include "../../lib/ble_navigation/workout_telemetry_state.hpp"
 #include "../../lib/gui/src/ride_stats_widget.hpp"
diff --git a/tools/generate_ride_ble_contract.py b/tools/generate_ride_ble_contract.py
index 099a9f1..c4bbb63 100755
--- a/tools/generate_ride_ble_contract.py
+++ b/tools/generate_ride_ble_contract.py
@@ -342,10 +342,14 @@ def render_zone_contract(contract: dict, swift: bool) -> str:
                 "nonisolated enum WorkoutZoneWireV1 {\n"
                 + "".join(f"    static let {camel(key)} = {value}\n" for key, value in values.items())
                 + "}\n")
+    # Firmware defines VERSION as its build-identity string. A namespace does
+    # not protect a C++ identifier from preprocessor macro expansion.
+    cpp_values = {("zone_wire_version" if key == "version" else key): value
+                  for key, value in values.items()}
     return ("// Generated by tools/generate_ride_ble_contract.py. Do not edit.\n"
             "#pragma once\n#include <cstddef>\n#include <cstdint>\n"
             "namespace workout_zone_wire {\n"
-            + "".join(f"inline constexpr uint32_t {upper(key)} = {value};\n" for key, value in values.items())
+            + "".join(f"inline constexpr uint32_t {upper(key)} = {value};\n" for key, value in cpp_values.items())
             + "} // namespace workout_zone_wire\n")
 
 
