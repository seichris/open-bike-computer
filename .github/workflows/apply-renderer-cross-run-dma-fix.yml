name: Apply corrected renderer cross-run memory gate

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Bootstrap paused while the deterministic mutation is corrected."
