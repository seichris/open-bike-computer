name: Paused renderer memory session phase bootstrap

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by the consolidated DMA attribution workflow."
