name: Zone protocol staging
on:
  push:
    branches: [work/healthkit-zones-research-20260915]
    paths: [.github/workflows/healthkit-zone-research.yml]
permissions:
  contents: read
jobs:
  stage:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: echo 'Versioned zone validation inputs are being staged.'
