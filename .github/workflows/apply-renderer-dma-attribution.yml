name: Apply renderer DMA attribution

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Bootstrap paused while the deterministic mutation is consolidated."
